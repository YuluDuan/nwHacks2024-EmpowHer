import React from "react";

const ProfileForm = ({
  firstName,
  lastName,
  email,
}: {
  firstName: string;
  lastName: string;
  email: string;
}) => {
  return <div>ProfileForm</div>;
};

export default ProfileForm;
